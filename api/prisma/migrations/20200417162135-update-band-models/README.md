# Migration `20200417162135-update-band-models`

This migration has been generated by andrewhemans at 4/17/2020, 4:21:35 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_Band" (
    "about" TEXT NOT NULL  ,
    "active" BOOLEAN NOT NULL DEFAULT true ,
    "authorId" INTEGER   ,
    "bandName" TEXT NOT NULL  ,
    "bandcamp" TEXT NOT NULL  ,
    "createdAt" DATE NOT NULL DEFAULT CURRENT_TIMESTAMP ,
    "genre" TEXT NOT NULL  ,
    "homeTown" TEXT NOT NULL  ,
    "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,
    "instagram" TEXT NOT NULL  ,
    "updatedAt" DATE NOT NULL  ,
    "website" TEXT NOT NULL  ,FOREIGN KEY ("authorId") REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE
) 

INSERT INTO "quaint"."new_Band" ("active", "authorId", "bandName", "createdAt", "id", "updatedAt") SELECT "active", "authorId", "bandName", "createdAt", "id", "updatedAt" FROM "quaint"."Band"

DROP TABLE "quaint"."Band";

ALTER TABLE "quaint"."new_Band" RENAME TO "Band";

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200417161222-schema-update..20200417162135-update-band-models
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource DS {
   provider = "sqlite"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider      = "prisma-client-js"
@@ -21,8 +21,14 @@
   createdAt  DateTime   @default(now())
   updatedAt  DateTime   @updatedAt
   active     Boolean    @default(true)
   bandName   String
+  homeTown   String
+  about      String
+  genre      String
+  website    String
+  bandcamp   String
+  instagram  String
   author     User?      @relation(fields:  [authorId], references: [id])
   authorId   Int?
 }
```


