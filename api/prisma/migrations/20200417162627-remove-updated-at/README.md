# Migration `20200417162627-remove-updated-at`

This migration has been generated by andrewhemans at 4/17/2020, 4:26:27 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_Stream" (
    "authorId" INTEGER   ,
    "body" TEXT NOT NULL  ,
    "createdAt" DATE NOT NULL DEFAULT CURRENT_TIMESTAMP ,
    "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,
    "link" TEXT NOT NULL  ,
    "title" TEXT NOT NULL  ,
    "when" DATE NOT NULL DEFAULT CURRENT_TIMESTAMP ,FOREIGN KEY ("authorId") REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE
) 

INSERT INTO "quaint"."new_Stream" ("authorId", "body", "createdAt", "id", "link", "title", "when") SELECT "authorId", "body", "createdAt", "id", "link", "title", "when" FROM "quaint"."Stream"

DROP TABLE "quaint"."Stream";

ALTER TABLE "quaint"."new_Stream" RENAME TO "Stream";

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;

PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_Band" (
    "about" TEXT NOT NULL  ,
    "active" BOOLEAN NOT NULL DEFAULT true ,
    "authorId" INTEGER   ,
    "bandName" TEXT NOT NULL  ,
    "bandcamp" TEXT NOT NULL  ,
    "createdAt" DATE NOT NULL DEFAULT CURRENT_TIMESTAMP ,
    "genre" TEXT NOT NULL  ,
    "homeTown" TEXT NOT NULL  ,
    "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,
    "instagram" TEXT NOT NULL  ,
    "website" TEXT NOT NULL  ,FOREIGN KEY ("authorId") REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE
) 

INSERT INTO "quaint"."new_Band" ("about", "active", "authorId", "bandName", "bandcamp", "createdAt", "genre", "homeTown", "id", "instagram", "website") SELECT "about", "active", "authorId", "bandName", "bandcamp", "createdAt", "genre", "homeTown", "id", "instagram", "website" FROM "quaint"."Band"

DROP TABLE "quaint"."Band";

ALTER TABLE "quaint"."new_Band" RENAME TO "Band";

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200417162135-update-band-models..20200417162627-remove-updated-at
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource DS {
   provider = "sqlite"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider      = "prisma-client-js"
@@ -18,9 +18,8 @@
 model Band {
   id         Int        @id @default(autoincrement())
   createdAt  DateTime   @default(now())
-  updatedAt  DateTime   @updatedAt
   active     Boolean    @default(true)
   bandName   String
   homeTown   String
   about      String
@@ -34,9 +33,8 @@
 model Stream {
   id         Int        @id @default(autoincrement())
   createdAt  DateTime   @default(now())
-  updatedAt  DateTime   @updatedAt
   title      String
   body       String
   when       DateTime @default(now())
   link       String
```


