# Migration `20200417161222-schema-update`

This migration has been generated by andrewhemans at 4/17/2020, 4:12:22 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "quaint"."User" (
    "createdAt" DATE NOT NULL DEFAULT CURRENT_TIMESTAMP ,
    "email" TEXT NOT NULL  ,
    "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,
    "name" TEXT   
) 

CREATE TABLE "quaint"."Band" (
    "active" BOOLEAN NOT NULL DEFAULT true ,
    "authorId" INTEGER   ,
    "bandName" TEXT NOT NULL  ,
    "createdAt" DATE NOT NULL DEFAULT CURRENT_TIMESTAMP ,
    "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,
    "updatedAt" DATE NOT NULL  ,FOREIGN KEY ("authorId") REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE
) 

CREATE TABLE "quaint"."Stream" (
    "authorId" INTEGER   ,
    "body" TEXT NOT NULL  ,
    "createdAt" DATE NOT NULL DEFAULT CURRENT_TIMESTAMP ,
    "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,
    "link" TEXT NOT NULL  ,
    "title" TEXT NOT NULL  ,
    "updatedAt" DATE NOT NULL  ,
    "when" DATE NOT NULL DEFAULT CURRENT_TIMESTAMP ,FOREIGN KEY ("authorId") REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE
) 

CREATE UNIQUE INDEX "quaint"."User.email" ON "User"("email")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200417161222-schema-update
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,39 @@
+datasource DS {
+  provider = "sqlite"
+  url      = env("DATABASE_URL")
+}
+
+generator client {
+  provider      = "prisma-client-js"
+  binaryTargets = env("BINARY_TARGET")
+}
+
+model User {
+  id        Int      @id @default(autoincrement())
+  createdAt DateTime @default(now())
+  email     String   @unique
+  name      String?
+  bands     Band[]
+}
+
+model Band {
+  id         Int        @id @default(autoincrement())
+  createdAt  DateTime   @default(now())
+  updatedAt  DateTime   @updatedAt
+  active     Boolean    @default(true)
+  bandName   String
+  author     User?      @relation(fields:  [authorId], references: [id])
+  authorId   Int?
+}
+
+model Stream {
+  id         Int        @id @default(autoincrement())
+  createdAt  DateTime   @default(now())
+  updatedAt  DateTime   @updatedAt
+  title      String
+  body       String
+  when       DateTime @default(now())
+  link       String
+  author     User?      @relation(fields:  [authorId], references: [id])
+  authorId   Int?
+}
```


